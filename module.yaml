# Autogenerated definition - see docs.skylarkos.com for more
id: devuser
type: other
name: Development User Module
description: Provides a devuser:devuser account.
version: 0.1.0
importance: low
dependencies:
  modules:
    - id: base
filesystem:
  hooks:
    - path: filesystem/build.sh
      event: build
  modifications:
    manual:
    - path: /etc/subgid
      action: text-append
    - path: /etc/subuid
      action: text-append
    - path: /etc/passwd
      action: text-append
    - path: /etc/shadow
      action: text-append
